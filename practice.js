bookArray = [['abc',100],
             ['xyz',200],
             ['pqr',300]]

             bookArray.forEach((element, index) => {
                // index = bookArray.indexOf(bookArray[element[0]])
                console.log(index)

                
             });