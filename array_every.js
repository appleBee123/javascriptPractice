var a = [10,20,30,40,50,60]
var c = [{pname:"cleanser",pprice:200},{pname:"serum",pprice:300},{pname:"mist",pprice:600}]

// console.log(c.filter( (l) => (l.pprice>200))) 
console.log(a.find( (b) => (b>40))) 
// console.log(c.find( (l) => (l.pprice>200))) 
//  console.log(c.findIndex( (l) => (l.pprice>300))) 
 
// console.log(a.every( (b) => (b>20)))

// console.log(a.every( (a) => (a>20)))     // a is different in the parameter

// console.log(a.some( (b) => (b>50)))     

// console.log(a.reverse())     


//   console.log(a.filter( (b) => (b>20))) 

//   console.log(a)


// store results from above methods in a new array or explicitly in the same array

    
